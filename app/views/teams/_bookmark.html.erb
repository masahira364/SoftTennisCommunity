<% if team.bookmark_by?(current_user) %>
	<%= link_to "ブックマークを外す", team_bookmarks_path(team.id), method: :delete,remote: true, class: "btn btn-primary btn-block" %>
<% else %>
	<%= link_to "ブックマークする", team_bookmarks_path(team.id), method: :post, remote: true, class: "btn btn-primary btn-block" %>
<% end %>